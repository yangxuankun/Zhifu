server:
  port: 8007

spring:
  application:
    name: zhifu-consul-order-8007
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}